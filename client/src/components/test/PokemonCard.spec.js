describe("PokemonCard component", () => {
  test("It should show loader when loading request")
  test("It should show correct data for correct pokemon")
  test("It should use props passed to execute query")
});
